{
  "name": "brag",
  "version": "1.0.0",
  "description": "notification functions for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/brag"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/brag/issues"
  },
  "homepage": "https://github.com/fishin/brag",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 10000 -L",
    "test": "lab -a code -t 100 -v -m 10000 -L -r json -o test.lab -r html -o test.html -r console -o stdout -r console -o test.log"
  },
  "dependencies": {
    "hoek": "2.x.x",
    "nodemailer": "1.3.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
